import:
  - control/keyboard/include/move
control:
  keyboard:
    move:    Mod1-Shift

  keyboard_action:
    ${move}-Delete: kill_all_clients_in_grouping

    ${move}-t: move_grouping_to_view_from_menu

