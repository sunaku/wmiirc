import:
  - control/action/resize

control:
  keyboard_action:
    ${resize}-k: grow_client_up
    ${resize}-j: grow_client_down
    ${resize}-h: grow_client_left
    ${resize}-l: grow_client_right
    
